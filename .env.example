# Optional: Groq API key for local development fallback.
# The primary flow requires the user to paste their key in the Settings screen.
# This env variable is only used as a convenience fallback when the settings key is empty.
GROQ_API_KEY=gsk_your_key_here
